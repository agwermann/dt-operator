---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: twincomponents.dtd.digitaltwin
spec:
  group: dtd.digitaltwin
  names:
    kind: TwinComponent
    listKind: TwinComponentList
    plural: twincomponents
    singular: twincomponent
  scope: Namespaced
  versions:
  - name: v0
    schema:
      openAPIV3Schema:
        description: TwinComponent is the Schema for the twincomponents API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TwinComponentSpec defines the desired state of TwinComponent
            properties:
              name:
                type: string
              parent:
                type: string
              schema:
                properties:
                  attributes:
                    items:
                      properties:
                        name:
                          type: string
                        type:
                          type: string
                      type: object
                    type: array
                  identifier:
                    type: string
                type: object
            type: object
          status:
            description: TwinComponentStatus defines the observed state of TwinComponent
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
