# permissions for end users to view messaginggateways.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: messaginggateway-viewer-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: dt-operator
    app.kubernetes.io/part-of: dt-operator
    app.kubernetes.io/managed-by: kustomize
  name: messaginggateway-viewer-role
rules:
- apiGroups:
  - core.digitaltwin
  resources:
  - messaginggateways
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - core.digitaltwin
  resources:
  - messaginggateways/status
  verbs:
  - get
